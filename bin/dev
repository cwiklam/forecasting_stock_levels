#!/usr/bin/env bash

npm rebuild esbuild

if ! command -v foreman &> /dev/null
then
  echo "Installing foreman..."
  gem install foreman
fi

rails db:create
rails db:migrate

foreman start -f Procfile.dev "$@"
